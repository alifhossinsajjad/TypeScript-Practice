# How to set up typescript 

1. install node js LTS version and download TypeScript 
2. then create ts config file with this command 
```
tsc --init
```
3. now configure the tsconfig file 
```
first of all edit the root dir .
then edit the outer dir .

```
4. Remember that folder structure You can keep it as happy as you want.
5. Then run the command with tsc 
6. Now write the code and run the tsc command in the your terminal 
6.Now Finished your ts configuration 